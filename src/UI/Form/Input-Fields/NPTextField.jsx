import { TextField } from '@mui/material';
import React from 'react';

const NPTextField = (props) => {
    
    return(
            <TextField {...props} fullWidth variant="outlined"/>
        )
}

export default NPTextField;